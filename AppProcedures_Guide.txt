GUÍA DE USO — AppProcedures.sql
Procedimientos almacenados DML para app.py
===========================================


1. sp_ins_paciente
------------------
Registra un paciente nuevo y lo asigna a una sede en un solo paso.

Parámetros:
  - p_id_paciente       : ID entero del paciente (se ingresa manualmente en el form)
  - p_nombre            : Nombre del paciente
  - p_apellido_p        : Apellido paterno
  - p_apellido_m        : Apellido materno
  - p_fecha_nacimiento  : Fecha de nacimiento (formato YYYY-MM-DD)
  - p_id_estado         : ID del estado inicial (1 = Activo, 2 = En Hospital, 3 = Baja)
  - p_id_sede           : ID de la sede donde se registra

Cómo se llama en app.py:
  db.execute("CALL sp_ins_paciente(%s, %s, %s, %s, %s, %s, %s)",
             (id_pac, nombre, apellido_p, apellido_m, fecha_nac, id_estado, id_sede))

Lo que hace internamente:
  Inserta el registro en la tabla pacientes y al mismo tiempo crea la entrada
  correspondiente en sede_pacientes con fecha y hora de ingreso actuales.
  El ID de sede_pacientes se calcula automáticamente dentro del procedimiento.

Ruta que lo usa: POST /pacientes/nuevo


---


2. sp_upd_paciente
------------------
Actualiza los datos personales y el estado de un paciente existente.

Parámetros:
  - p_id_paciente       : ID del paciente a modificar
  - p_nombre            : Nombre nuevo
  - p_apellido_p        : Apellido paterno nuevo
  - p_apellido_m        : Apellido materno nuevo
  - p_fecha_nacimiento  : Fecha de nacimiento nueva
  - p_id_estado         : Estado nuevo

Cómo se llama en app.py:
  db.execute("CALL sp_upd_paciente(%s, %s, %s, %s, %s, %s)",
             (id, nombre, apellido_p, apellido_m, fecha_nac, id_estado))

Lo que hace internamente:
  Hace un UPDATE sobre la tabla pacientes usando el ID como llave.
  No toca sede_pacientes ni ninguna otra tabla.

Ruta que lo usa: POST /pacientes/editar/<id>


---


3. sp_del_paciente
------------------
Realiza la baja lógica de un paciente (soft delete).

Parámetros:
  - p_id_paciente : ID del paciente a dar de baja

Cómo se llama en app.py:
  db.execute("CALL sp_del_paciente(%s)", (id,))

Lo que hace internamente:
  Cambia id_estado a 3 (Baja) en la tabla pacientes.
  El registro no se borra físicamente, solo queda excluido de las vistas activas.

Ruta que lo usa: POST /pacientes/eliminar/<id>


---


4. sp_ins_enfermedad
--------------------
Agrega un diagnóstico de enfermedad al historial de un paciente.

Parámetros:
  - p_id_paciente   : ID del paciente
  - p_id_enfermedad : ID de la enfermedad (catálogo enfermedades)
  - p_fecha_diag    : Fecha del diagnóstico (formato YYYY-MM-DD)

Cómo se llama en app.py:
  db.execute("CALL sp_ins_enfermedad(%s, %s, %s)", (id, id_enfermedad, fecha_diag))

Lo que hace internamente:
  Inserta una fila en tiene_enfermedad con la combinación paciente-enfermedad-fecha.
  Si la combinación ya existe la base de datos rechazará el INSERT (constraint único).

Ruta que lo usa: POST /pacientes/<id>/agregar-enfermedad


---


5. sp_del_enfermedad
--------------------
Elimina un diagnóstico del historial de un paciente.

Parámetros:
  - p_id_paciente   : ID del paciente
  - p_id_enfermedad : ID de la enfermedad a quitar

Cómo se llama en app.py:
  db.execute("CALL sp_del_enfermedad(%s, %s)", (id, id_enfermedad))

Lo que hace internamente:
  Borra la fila de tiene_enfermedad que coincida con ambos IDs.
  Si no existe la combinación simplemente no hace nada (DELETE sin coincidencia).

Ruta que lo usa: POST /pacientes/<id>/quitar-enfermedad


---


6. sp_ins_contacto
------------------
Registra un contacto de emergencia y lo vincula al paciente en un solo paso.

Parámetros:
  - p_id_paciente : ID del paciente al que pertenece el contacto
  - p_nombre      : Nombre del contacto
  - p_apellido_p  : Apellido paterno
  - p_apellido_m  : Apellido materno (puede ser NULL)
  - p_telefono    : Teléfono del contacto
  - p_relacion    : Relación con el paciente (ej. "Hijo", "Esposa")
  - p_email       : Correo electrónico (puede ser NULL)
  - p_pin_acceso  : PIN para acceso al portal familiar (puede ser NULL)

Cómo se llama en app.py:
  db.execute("CALL sp_ins_contacto(%s, %s, %s, %s, %s, %s, %s, %s)",
             (id, nombre, apellido_p, apellido_m, telefono, relacion, email, pin_acceso))

Lo que hace internamente:
  Calcula el siguiente ID disponible en contactos_emergencia.
  Calcula la siguiente prioridad para ese paciente en paciente_contactos.
  Inserta en contactos_emergencia y luego en paciente_contactos con la prioridad correcta.
  Todo en una sola transacción.

Ruta que lo usa: POST /pacientes/<id>/agregar-contacto


---


7. sp_ins_kit
-------------
Asigna un dispositivo GPS a un paciente.

Parámetros:
  - p_id_paciente        : ID del paciente
  - p_id_dispositivo_gps : ID del dispositivo GPS a asignar

Cómo se llama en app.py:
  db.execute("CALL sp_ins_kit(%s, %s)", (id, id_gps))

Lo que hace internamente:
  Calcula el siguiente ID para asignacion_kit e inserta la fila con fecha de entrega actual.
  La base de datos tiene un índice parcial (uq_gps_activo) que impide asignar el mismo
  GPS a dos pacientes al mismo tiempo. Si el dispositivo ya está activo en otra asignación,
  el procedimiento fallará con un error de constraint.

Ruta que lo usa: POST /pacientes/<id>/asignar-kit


---


8. sp_ins_turno
---------------
Registra un nuevo turno de cuidador en una zona.

Parámetros:
  - p_id_turno    : ID del turno (se ingresa manualmente en el form)
  - p_id_cuidador : ID del cuidador (empleado)
  - p_id_zona     : ID de la zona asignada
  - p_hora_inicio : Hora de inicio del turno (formato HH:MM)
  - p_hora_fin    : Hora de fin del turno (formato HH:MM)
  - p_lunes       : TRUE si el turno aplica los lunes
  - p_martes      : TRUE si aplica los martes
  - p_miercoles   : TRUE si aplica los miércoles
  - p_jueves      : TRUE si aplica los jueves
  - p_viernes     : TRUE si aplica los viernes
  - p_sabado      : TRUE si aplica los sábados
  - p_domingo     : TRUE si aplica los domingos

Cómo se llama en app.py:
  db.execute("CALL sp_ins_turno(%s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s)",
             (id_turno, id_cuidador, id_zona, hora_inicio, hora_fin,
              dias["lunes"], dias["martes"], dias["miercoles"], dias["jueves"],
              dias["viernes"], dias["sabado"], dias["domingo"]))

Lo que hace internamente:
  Inserta la fila en turno_cuidador con activo = TRUE por defecto.
  Los días de la semana vienen del formulario como checkboxes; si no se marcó un día
  llega como False desde Python.

Ruta que lo usa: POST /turnos/nuevo


---


9. sp_upd_turno
---------------
Actualiza un turno existente incluyendo el flag activo.

Parámetros:
  - p_id_turno    : ID del turno a modificar
  - p_id_cuidador : Nuevo cuidador asignado
  - p_id_zona     : Nueva zona
  - p_hora_inicio : Nueva hora de inicio
  - p_hora_fin    : Nueva hora de fin
  - p_lunes       : Boolean lunes
  - p_martes      : Boolean martes
  - p_miercoles   : Boolean miércoles
  - p_jueves      : Boolean jueves
  - p_viernes     : Boolean viernes
  - p_sabado      : Boolean sábado
  - p_domingo     : Boolean domingo
  - p_activo      : TRUE para turno activo, FALSE para desactivarlo sin borrarlo

Cómo se llama en app.py:
  db.execute("CALL sp_upd_turno(%s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s)",
             (id, id_cuidador, id_zona, hora_inicio, hora_fin,
              dias["lunes"], dias["martes"], dias["miercoles"], dias["jueves"],
              dias["viernes"], dias["sabado"], dias["domingo"], activo))

Lo que hace internamente:
  Hace UPDATE sobre turno_cuidador usando id_turno como llave.
  El campo activo permite desactivar un turno sin eliminarlo, útil para
  suspensiones temporales de cuidadores.

Ruta que lo usa: POST /turnos/editar/<id>


---


10. sp_del_turno
----------------
Elimina un turno de forma permanente.

Parámetros:
  - p_id_turno : ID del turno a eliminar

Cómo se llama en app.py:
  db.execute("CALL sp_del_turno(%s)", (id,))

Lo que hace internamente:
  DELETE directo sobre turno_cuidador. A diferencia del paciente, el turno
  sí se borra físicamente porque no tiene historial que preservar.

Ruta que lo usa: POST /turnos/eliminar/<id>




---


11. sp_transferir_sede
----------------------
Transfiere un paciente de su sede actual a una nueva de forma atómica.

Parámetros:
  - p_id_paciente   : ID del paciente a transferir
  - p_nueva_sede_id : ID de la sede destino

Cómo se llama en app.py:
  db.execute("CALL sp_transferir_sede(%s, %s)", (id, nueva_sede_id))

Lo que hace internamente:
  Cierra la fila activa en sede_pacientes (fecha_salida y hora_salida = NOW).
  Calcula el siguiente ID y abre una nueva fila para la sede destino.
  Todo en una sola transacción — nunca queda el paciente sin sede ni con dos activas.
  La validación de misma-sede y de múltiples activas sigue haciéndose en Python antes de llamar al SP.

Ruta que lo usa: POST /pacientes/<id>/transferir-sede


---


12. sp_upd_alerta_atendida
--------------------------
Marca una alerta como atendida.

Parámetros:
  - p_id_alerta : ID de la alerta

Cómo se llama en app.py:
  db.execute("CALL sp_upd_alerta_atendida(%s)", (id,))

Lo que hace internamente:
  UPDATE alertas SET estatus = 'Atendida'. No toca fecha_hora ni ningún otro campo.

Ruta que lo usa: POST /alertas/resolver/<id>


---


13. sp_del_alerta
-----------------
Elimina una alerta permanentemente.

Parámetros:
  - p_id_alerta : ID de la alerta a eliminar

Cómo se llama en app.py:
  db.execute("CALL sp_del_alerta(%s)", (id,))

Lo que hace internamente:
  DELETE directo sobre alertas. Las alertas generadas por triggers pueden eliminarse
  desde la UI pero normalmente se marcan como Atendidas en lugar de borrarse.

Ruta que lo usa: POST /alertas/eliminar/<id>


---


14. sp_ins_dispositivo
----------------------
Registra un nuevo dispositivo IoT (GPS, BEACON o NFC).

Parámetros:
  - p_id_dispositivo : ID entero (manual, no SERIAL)
  - p_id_serial      : Serial único del dispositivo
  - p_tipo           : Exactamente 'GPS', 'BEACON' o 'NFC'
  - p_modelo         : Modelo del dispositivo

Cómo se llama en app.py:
  db.execute("CALL sp_ins_dispositivo(%s, %s, %s, %s)", (id_disp, id_serial, tipo, modelo))

Lo que hace internamente:
  INSERT en dispositivos con estado = 'Activo' por defecto.
  El tipo y el serial tienen constraints únicos — si se duplica el serial la DB rechaza el INSERT.

Ruta que lo usa: POST /dispositivos/nuevo


---


15. sp_upd_dispositivo
----------------------
Actualiza los datos de un dispositivo existente.

Parámetros:
  - p_id_dispositivo : ID del dispositivo a modificar
  - p_id_serial      : Serial nuevo
  - p_tipo           : Tipo nuevo
  - p_modelo         : Modelo nuevo
  - p_estado         : Estado nuevo ('Activo', 'Inactivo', etc.)

Cómo se llama en app.py:
  db.execute("CALL sp_upd_dispositivo(%s, %s, %s, %s, %s)", (id, id_serial, tipo, modelo, estado))

Lo que hace internamente:
  UPDATE sobre dispositivos usando id_dispositivo como llave.

Ruta que lo usa: POST /dispositivos/editar/<id>


---


16. sp_del_dispositivo
----------------------
Elimina un dispositivo permanentemente.

Parámetros:
  - p_id_dispositivo : ID del dispositivo a eliminar

Cómo se llama en app.py:
  db.execute("CALL sp_del_dispositivo(%s)", (id,))

Lo que hace internamente:
  DELETE directo. Fallará si el dispositivo tiene lecturas_gps, detecciones_beacon,
  lecturas_nfc o asignacion_kit activas (FK con ON DELETE RESTRICT).

Ruta que lo usa: POST /dispositivos/eliminar/<id>


---


17. sp_ins_zona
---------------
Registra una nueva zona segura.

Parámetros:
  - p_nombre_zona   : Nombre de la zona
  - p_latitud       : Latitud del centro (WGS-84)
  - p_longitud      : Longitud del centro (WGS-84)
  - p_radio_metros  : Radio de la zona en metros

Cómo se llama en app.py:
  db.execute("CALL sp_ins_zona(%s, %s, %s, %s)", (nombre_zona, latitud, longitud, radio))

Lo que hace internamente:
  INSERT en zonas. El id_zona es SERIAL — la DB lo asigna automáticamente.
  El trigger trg_zona_exit_gps usará esta zona en su próxima ejecución.

Ruta que lo usa: POST /zonas/nueva


---


18. sp_upd_zona
---------------
Actualiza una zona segura existente.

Parámetros:
  - p_id_zona       : ID de la zona a modificar
  - p_nombre_zona   : Nombre nuevo
  - p_latitud       : Latitud nueva
  - p_longitud      : Longitud nueva
  - p_radio_metros  : Radio nuevo en metros

Cómo se llama en app.py:
  db.execute("CALL sp_upd_zona(%s, %s, %s, %s, %s)", (id, nombre_zona, latitud, longitud, radio))

Lo que hace internamente:
  UPDATE sobre zonas. Cambiar el radio afecta directamente el comportamiento
  del trigger trg_zona_exit_gps en la próxima lectura GPS.

Ruta que lo usa: POST /zonas/editar/<id>


---


19. sp_del_zona
---------------
Elimina una zona segura permanentemente.

Parámetros:
  - p_id_zona : ID de la zona a eliminar

Cómo se llama en app.py:
  db.execute("CALL sp_del_zona(%s)", (id,))

Lo que hace internamente:
  DELETE directo sobre zonas. Fallará si hay sede_zonas o beacon_zona vinculados
  (FK con ON DELETE RESTRICT).

Ruta que lo usa: POST /zonas/eliminar/<id>


---


20. sp_upd_cerrar_asignacion_beacon
------------------------------------
Cierra la asignación activa de un beacon a un cuidador.

Parámetros:
  - p_id_asignacion : ID de la asignación a cerrar

Cómo se llama en app.py:
  db.execute("CALL sp_upd_cerrar_asignacion_beacon(%s)", (id,))

Lo que hace internamente:
  UPDATE asignacion_beacon SET fecha_fin = CURRENT_DATE.
  Al tener fecha_fin, los índices parciales uq_beacon_activo_por_cuidador y
  uq_beacon_dispositivo_activo ya no la consideran activa, permitiendo
  reasignar el beacon o el cuidador.

Ruta que lo usa: POST /equipamiento/asignacion-beacons/<id>/cerrar



21. sp_ins_cuidador
-------------------
Registra un cuidador nuevo: inserta en empleados y en cuidadores en una sola transacción.

Parámetros:
  - p_id_empleado  : ID entero (ingresado manualmente)
  - p_nombre       : Nombre
  - p_apellido_p   : Apellido paterno
  - p_apellido_m   : Apellido materno
  - p_curp         : CURP o pasaporte
  - p_telefono     : Teléfono (NULL permitido)

Cómo se llama en app.py:
  db.execute("CALL sp_ins_cuidador(%s, %s, %s, %s, %s, %s)",
             (id_cuid, nombre, apellido_p, apellido_m, curp, telefono))

Ruta que lo usa: POST /cuidadores/nuevo


22. sp_upd_cuidador
-------------------
Actualiza los datos de un empleado/cuidador existente.

Parámetros: p_id_empleado, p_nombre, p_apellido_p, p_apellido_m, p_curp, p_telefono

Cómo se llama en app.py:
  db.execute("CALL sp_upd_cuidador(%s, %s, %s, %s, %s, %s)",
             (id, nombre, apellido_p, apellido_m, curp, telefono))

Ruta que lo usa: POST /cuidadores/editar/<id>


23. sp_del_cuidador
-------------------
Da de baja un cuidador: DELETE en cuidadores y luego en empleados (FK dependency).

Parámetros: p_id_empleado

Cómo se llama en app.py:
  db.execute("CALL sp_del_cuidador(%s)", (id,))

Ruta que lo usa: POST /cuidadores/eliminar/<id>


24. sp_ins_alerta
-----------------
Inserta una alerta manual con auto-incremento de ID.

Parámetros:
  - p_id_paciente  : INT (NULL para alertas de zona sin paciente)
  - p_tipo_alerta  : VARCHAR — valor de cat_tipo_alerta
  - p_fecha_hora   : TIMESTAMP

Cómo se llama en app.py:
  db.execute("CALL sp_ins_alerta(%s, %s, %s)", (id_paciente, tipo_alerta, fecha_hora))

Lo que hace internamente:
  Calcula COALESCE(MAX(id_alerta), 0)+1, inserta con estatus='Activa'.

Ruta que lo usa: POST /alertas/nueva


25. sp_upd_stock
----------------
Ajusta el stock de un medicamento en una sede.

Parámetros: p_gtin, p_id_sede, p_stock_nuevo

Cómo se llama en app.py:
  db.execute("CALL sp_upd_stock(%s, %s, %s)", (gtin, id_sede, stock_nuevo))

Ruta que lo usa: POST /farmacia/inventario/ajustar


26. sp_ins_suministro
---------------------
Crea la cabecera de una orden de suministro.

Parámetros: p_id_suministro, p_id_farmacia, p_id_sede, p_fecha_entrega, p_estado

Cómo se llama en app.py:
  db.execute("CALL sp_ins_suministro(%s, %s, %s, %s, %s)",
             (id_sum, id_farmacia, id_sede, fecha, estado))

Ruta que lo usa: POST /farmacia/suministro/nuevo


27. sp_ins_suministro_linea
---------------------------
Agrega o acumula una línea de medicamento a una orden de suministro.
Usa ON CONFLICT para sumar cantidad si el GTIN ya existe en esa orden.

Parámetros: p_id_suministro, p_gtin, p_cantidad

Cómo se llama en app.py:
  db.execute("CALL sp_ins_suministro_linea(%s, %s, %s)", (id_sum, gtin, cantidad))

Rutas que lo usan:
  POST /farmacia/suministro/nuevo (bucle de líneas)
  POST /farmacia/suministro/<id>/linea/agregar


28. sp_upd_suministro_estado
-----------------------------
Actualiza el estado de una orden de suministro.

Parámetros: p_id_suministro, p_estado

Cómo se llama en app.py:
  db.execute("CALL sp_upd_suministro_estado(%s, %s)", (id, nuevo_estado))

Ruta que lo usa: POST /farmacia/suministro/<id>/estado


29. sp_del_suministro
---------------------
Elimina una orden de suministro (CASCADE borra las líneas).

Parámetros: p_id_suministro

Cómo se llama en app.py:
  db.execute("CALL sp_del_suministro(%s)", (id,))

Ruta que lo usa: POST /farmacia/suministro/<id>/eliminar


30. sp_ins_visita
-----------------
Registra una visita de familiar/visitante a un paciente.

Parámetros: p_id_visita, p_id_paciente, p_id_visitante, p_id_sede, p_fecha_entrada, p_hora_entrada

Cómo se llama en app.py:
  db.execute("CALL sp_ins_visita(%s, %s, %s, %s, %s, %s)",
             (id_visita, id_paciente, id_visitante, id_sede, fecha, hora))

Ruta que lo usa: POST /visitas/nueva


31. sp_ins_lectura_gps
-----------------------
Inserta una lectura GPS con cálculo automático de geom (PostGIS).
Dispara trg_zona_exit_gps y trg_bateria_baja_gps automáticamente.

Parámetros:
  - p_id_dispositivo  : ID del dispositivo GPS
  - p_latitud         : DOUBLE PRECISION
  - p_longitud        : DOUBLE PRECISION
  - p_nivel_bateria   : INT
  - p_altura          : DOUBLE PRECISION (NULL si no se conoce)

Cómo se llama en app.py:
  db.execute("CALL sp_ins_lectura_gps(%s, %s, %s, %s, %s)",
             (id_dispositivo, latitud, longitud, nivel_bateria, altura))

Lo que hace internamente:
  Calcula COALESCE(MAX(id_lectura), 0)+1, inserta con geom via
  ST_SetSRID(ST_MakePoint(longitud, latitud), 4326)::geography.

Rutas que lo usan:
  POST /sim/gps (simulador de lectura)
  POST /api/gps/lectura (API IoT)


===========================================
Total: 31 procedimientos — pacientes, cuidadores, turnos, alertas, dispositivos, zonas, beacon, farmacia, visitas, GPS
Archivo SQL: AppProcedures.sql
Aplicar con: psql -U alzadmin -d alzheimer -f AppProcedures.sql
